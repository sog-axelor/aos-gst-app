<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<object-views xmlns="http://axelor.com/xml/ns/object-views"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://axelor.com/xml/ns/object-views
  http://axelor.com/xml/ns/object-views/object-views_5.4.xsd">

  <grid model="com.axelor.apps.account.db.Invoice" title="Invoice" name="invoice-grid"
    extension="true" id="gst-invoice-grid">
    <extend target="//panel-related[@name='invoiceLineTaxListPanel']">
      <insert position="before">
        <field name="priority" if="__config__.app.isApp('gst')"/>
        <field name="netIgst" if="__config__.app.isApp('gst')"/>
        <field name="netCgst" if="__config__.app.isApp('gst')"/>
        <field name="netSgst" if="__config__.app.isApp('gst')"/>
      </insert>
    </extend>
  </grid>

  <form model="com.axelor.apps.account.db.Invoice" title="Invoice" name="invoice-form"
    extension="true" id="gst-invoice-form">
    <extend target="//panel-related[@name='invoiceLineTaxListPanel']">
      <insert position="before">
        <field name="priority" if="__config__.app.isApp('gst')"/>
        <field name="netIgst" if="__config__.app.isApp('gst')"/>
        <field name="netCgst" if="__config__.app.isApp('gst')"/>
        <field name="netSgst" if="__config__.app.isApp('gst')"/>
      </insert>
    </extend>

    <extend target="//panel-related[@name='invoiceLineListPanel']">
      <attribute name="onChange" value="action-gst-report-print"/>
    </extend>

  </form>

  <action-method name="action-gst-invoice-calculations">
    <call class="com.axelor.apps.gst.controller.GstController" method="invoiceGst"/>
  </action-method>

  <action-group name="action-group-invoice-onChange">
    <action name="action-invoice-group-invoice-line-list-on-change"/>
  </action-group>

  <action-method name="action-gst-report-print">
    <call class="com.axelor.apps.gst.impl.invoiceGstPrintReportImpl"
      method="prepareReportSettings"/>
  </action-method>



</object-views>
